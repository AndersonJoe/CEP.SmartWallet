apiVersion: v1
kind: Service
metadata:
  name: cep-smartwallet-db
  namespace: cep-smartwallet
spec:
  selector:
    app: cep-smartwallet-db
  ports:
    - port: 1433
      targetPort: 1433
  type: ClusterIP
