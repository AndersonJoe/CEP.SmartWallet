apiVersion: v1
kind: Service
metadata:
  name: cep-smartwallet-ui
  namespace: cep-smartwallet
spec:
  selector:
    app: cep-smartwallet-ui
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30010
  type: NodePort